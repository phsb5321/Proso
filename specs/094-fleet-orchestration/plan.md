# Plan — Feature 094

1. Inspect every active worktree, actual branch, commit, and dirty state.
2. Publish the single owner map in `.agents/blackboard.md`.
3. Record gate exclusivity, independent-review routing, workflow gating, and
   the product-policy stop condition.
4. Validate that the documentation is internally consistent and does not alter
   implementation worktrees.

## Constitution check

This is a coordination-only documentation slice. It changes no runtime code,
permissions, network flow, storage schema, or product invariant. The map
records—not resolves—the existing INV-001/INV-002 entitlement conflict.
