# Plan

1. Reuse the existing delivery harness and browser/property dependencies.
2. Add a repository-scoped Codex skill with actor/observer/verdict boundaries.
3. Preserve the internal-dispatch Firefox route as `user-gate-diagnostic`.
4. Fail `make user-gate` until a public Firefox actor satisfies the merged
   Feature 095 outcome contract.
5. Record exact-HEAD journey status, explicit falsifiers, and the Pedro-gated
   Free managed-TTS entitlement without simulating it in the fixture.
