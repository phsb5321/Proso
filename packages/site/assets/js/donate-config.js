/**
 * Donation configuration.
 *
 * Fill these in and deploy — the pages render only what exists:
 *   pixKey      PIX key (prefer a random key or e-mail; never publish a CPF)
 *   pixName     Beneficiary name shown to the donor (bank confirmation)
 *   pixCity     Beneficiary city
 *   pixPayload  Optional full "copia e cola" BR Code (preferred over the key)
 *   pixQrImage  Optional path to a static QR image (e.g. assets/images/pix-qr.png)
 *   kofiUrl     Ko-fi page URL once created
 *   githubSponsorsUrl  GitHub Sponsors URL once approved
 */
window.PROSO_DONATE = {
  pixKey: '',
  pixName: '',
  pixCity: '',
  pixPayload: '',
  pixQrImage: '',
  kofiUrl: '',
  githubSponsorsUrl: '',
};
