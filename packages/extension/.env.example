# Proso Environment Variables
# Copy this file to .env and fill in the values

# Telemetry Gateway Configuration
# These are injected at build time via Vite define and seeded into browser.storage.local at install
TELEMETRY_GATEWAY_URL=https://logs.proso.com/ingest
TELEMETRY_GATEWAY_TOKEN=
