// SPDX-License-Identifier: AGPL-3.0-or-later

/**
 * Stub for Vite/UnoCSS virtual modules (`virtual:uno.css`).
 *
 * Entrypoints import these for their styling side effect; Vite generates them at
 * build time, so Jest cannot resolve the specifier. Styling is not under test.
 */
export default {};
