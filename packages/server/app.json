{
  "name": "proso-api",
  "description": "Proso backend API server",
  "scripts": {
    "dokku": {
      "predeploy": "npx prisma migrate deploy"
    }
  },
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "web check",
        "path": "/health",
        "attempts": 10,
        "wait": 5
      }
    ]
  }
}
