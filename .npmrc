# Enforce pnpm as the package manager
engine-strict=true

# Auto-install peer dependencies
auto-install-peers=true

# Hoist all dependencies (compatibility with tools that expect node_modules structure)
shamefully-hoist=true
