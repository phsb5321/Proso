# Enforce pnpm as the package manager
engine-strict=true

# Auto-install peer dependencies
auto-install-peers=true

# Hoist NestJS packages to prevent DI duplicate issues (NestJS #13463)
public-hoist-pattern[]=@nestjs/*
