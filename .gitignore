# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.*
crash.log
crash.*.log
# Never commit variable files — they carry account ids and, by accident, secrets.
*.tfvars
*.tfvars.json
!example.tfvars
.terraformrc
terraform.rc
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local tooling
.direnv/
result
result-*

# Bootstrap phase-1 escape hatch (see stacks/00-bootstrap/README.md)
backend.tf.bootstrap-off
